<% if @sub_categories %>
<li><hr class="dropdown-divider"></li>
<% @sub_categories.each do |category| %>
    
    <li class="dropdown-item"><%= link_to category.name, products_path(category_id:  category.id) ,class: "" %></li>
<% end %>
<% end %>